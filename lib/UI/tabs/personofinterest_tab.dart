import 'package:flutter/material.dart';

class PersonOfInterestTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Interests'),
    );
  }
}
